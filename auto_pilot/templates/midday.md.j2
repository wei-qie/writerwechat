# 基市红绿灯 | 午间收盘

**{{ date_str }}（周{{ weekday }}）**

---

## 半日综述

**三大指数** — {% for idx in indices %}{{ idx.name }} {{ "%.2f"|format(idx.change_pct) }}%{% if not loop.last %} | {% endif %}{% endfor %}

盘面上涨 **{{ stats.up }}** 只，下跌 **{{ stats.down }}** 只；涨停 {{ stats.limit_up }} 只，跌停 {{ stats.limit_down }} 只。

**板块方面**，{% if sectors_gain %}{{ sectors_gain[:4]|map(attribute='name')|join('、') }}走强；{% endif %}{% if sectors_lose %}{{ sectors_lose[:4]|map(attribute='name')|join('、') }}走弱。{% endif %}

{% set total_fund = fund_gain|length + fund_lose|length %}
{% if total_fund > 0 %}
**基金方面**，盘中 {{ fund_gain|length }} 只基金上涨、{{ fund_lose|length }} 只基金下跌。
{% endif %}

---

*数据来源：腾讯财经、天天基金 | 仅供参考，不构成投资建议*
