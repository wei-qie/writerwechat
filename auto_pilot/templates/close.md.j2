# 基市红绿灯 | 收盘总结

**{{ date_str }}（周{{ weekday }}）**

---

{{ summary }}

**三大指数** — {% for idx in indices %}{{ idx.name }} {{ "%.2f"|format(idx.change_pct) }}%{% if not loop.last %} | {% endif %}{% endfor %}

**个股表现** — 上涨 **{{ stats.up }}** 只，下跌 **{{ stats.down }}** 只；涨停 {{ stats.limit_up }} 只，跌停 {{ stats.limit_down }} 只。

**板块方面**，{% if sectors_gain %}{{ sectors_gain[:5]|map(attribute='name')|join('、') }}涨幅居前；{% endif %}{% if sectors_lose %}{{ sectors_lose[:5]|map(attribute='name')|join('、') }}跌幅居前。{% endif %}

{% if fund_gain or fund_lose %}
**基金表现**，{% if fund_gain %}涨幅前三：{% for f in fund_gain[:3] %}{{ f.name }}（{{ "%.2f"|format(f.daily_change) }}%）{% if not loop.last %}、{% endif %}{% endfor %}{% endif %}{% if fund_gain and fund_lose %}；{% endif %}{% if fund_lose %}跌幅前三：{% for f in fund_lose[:3] %}{{ f.name }}（{{ "%.2f"|format(f.daily_change) }}%）{% if not loop.last %}、{% endif %}{% endfor %}{% endif %}。
{% endif %}

---

> {{ summary }}

---

*数据来源：腾讯财经、天天基金 | 仅供参考，不构成投资建议*
