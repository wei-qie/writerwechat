# 芯元财经速递 | 盘前预览

**{{ date_str }}（周{{ weekday }}）**

---

## A股开盘

{{ open_desc }}。开盘约 {{ stats.down }} 只个股下跌。

**板块方面**，{% if sectors_gain %}{{ sectors_gain[:3]|map(attribute='name')|join('、') }}等走强；{% endif %}{% if sectors_lose %}{{ sectors_lose[:3]|map(attribute='name')|join('、') }}等跌幅居前。{% endif %}

---

## 隔夜外盘

{% if us_indices %}
**美股** — {% for idx in us_indices %}{{ idx.name }} {{ "%.2f"|format(idx.change_pct) }}%{% if not loop.last %} | {% endif %}{% endfor %}
{% endif %}

{% if asia_indices %}
**亚太** — {% for idx in asia_indices %}{{ idx.name }} {{ "%.2f"|format(idx.change_pct) }}%{% if not loop.last %} | {% endif %}{% endfor %}
{% endif %}

---

*数据来源：腾讯财经、天天基金 | 仅供参考，不构成投资建议*
